[project]
name = "tracker-expense-mcp-server"
version = "0.1.0"
description = "MCP Server for Expense Tracker App"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aiosqlite>=0.21.0",
    "fastmcp>=2.14.2",
    "motor>=3.3.1",
    "python-dotenv>=1.0.0",
]
